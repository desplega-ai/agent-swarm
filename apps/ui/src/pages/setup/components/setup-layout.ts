/**
 * The one page column of `/setup`: header, step content, and footer share it,
 * so their edges line up and switching steps never moves them.
 */
export const SETUP_COLUMN = "mx-auto w-full max-w-[880px] px-3 sm:px-5";
