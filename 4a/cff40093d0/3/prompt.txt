# Review Document

A thin wrapper that invokes the `desplega:reviewing` skill with autonomy controls.

## When Invoked

1. **Parse flags from arguments:**
   - Check for `--autonomy=autopilot|critical|verbose` flag
   - Check for `--output=append|separate` flag (sets output mode preference, skipping the preference question)
   - Otherwise, default to **Critical** autonomy

2. **ALWAYS invoke the `desplega:reviewing` skill:**
   - Pass the document path
   - Pass the autonomy mode determined above
   - Pass the output mode if specified
   - Let the skill handle all review logic

3. **If no path provided:**
   - Respond with: "I need a document to review. Please provide the path to the research, plan, or brainstorm document you'd like me to review."

## Example Usage

```
/review thoughts/taras/research/2026-03-09-new-base-skills.md
/review --autonomy=autopilot --output=append thoughts/taras/plans/2026-03-09-feature.md
/review --output=separate thoughts/shared/brainstorms/2026-03-09-idea.md
```


ARGUMENTS: thoughts/taras/research/2026-03-18-workflow-redesign.md

---

Base directory for this skill: /Users/taras/.claude/plugins/cache/desplega-ai-toolbox/desplega/1.7.0/skills/reviewing

# Reviewing

You are performing a structured critique of a document (research, plan, or brainstorm) to identify gaps, weaknesses, and quality issues.

## Working Agreement

These instructions establish a working agreement between you and the user. The key principles are:

1. **AskUserQuestion is your primary communication tool** - Whenever you need to ask the user anything (clarifications, preferences, decisions), use the **AskUserQuestion tool**. Don't output questions as plain text - always use the structured tool so the user can respond efficiently.

2. **Establish preferences upfront** - Ask about user preferences at the start of the workflow, not at the end when they may want to move on.

3. **Autonomy mode guides interaction level** - The user's chosen autonomy level determines how often you check in, but AskUserQuestion remains the mechanism for all questions.

### User Preferences

Before starting review (unless autonomy is Autopilot), establish these preferences:

**Output Mode** - Use **AskUserQuestion** with:

| Question | Options |
|----------|---------|
| "How should I present the review findings?" | 1. Append errata section to the document (Recommended), 2. Write a separate review file to thoughts/*/reviews/ |

**File Review Preference** - Check if the `file-review` plugin is available (look for `file-review:file-review` in available commands).

If file-review plugin is installed, use **AskUserQuestion** with:

| Question | Options |
|----------|---------|
| "Would you like to use file-review for inline feedback after the automated review?" | 1. Yes, open file-review after review (Recommended), 2. No, the automated review is sufficient |

Store these preferences and act on them during the review process.

## When to Use

This skill activates when:
- User invokes `/review` command
- Another skill references the reviewing skill at completion
- User asks to review, critique, or check a document

## Autonomy Mode

At the start of review, adapt your interaction level based on the autonomy mode:

| Mode | Behavior |
|------|----------|
| **Autopilot** | Run full review, auto-fix minor issues, present summary at end |
| **Critical** (Default) | Ask about Critical/Important findings, auto-fix Minor ones |
| **Verbose** | Walk through each finding, confirm before any changes |

The autonomy mode is passed by the invoking command. If not specified, default to **Critical**.

## Process Steps

### Step 1: Read & Classify

Read the input document fully. Determine the document type from its path and content structure:

| Path contains | Type |
|---------------|------|
| `/research/` | Research document |
| `/plans/` | Plan |
| `/brainstorms/` | Brainstorm |
| `/qa/` | QA report |

If the type is ambiguous, infer from content structure or use **AskUserQuestion** to clarify.

### Step 2: Structural Check

Verify required sections exist based on document type:

**Research documents:**
- YAML frontmatter with required fields (date, researcher, topic, status)
- Research Question section
- Summary section
- Detailed Findings with file:line references
- Code References table
- Open Questions section

**Plans:**
- YAML frontmatter with required fields (date, planner, topic, status)
- Overview section
- Current State Analysis
- Desired End State with Key Discoveries
- What We're NOT Doing
- Phases with Changes Required and Success Criteria (both Automated and Manual Verification)
- Quick Verification Reference

**Brainstorms:**
- YAML frontmatter with required fields (date, author, topic, status)
- Context section
- Exploration section with Q&A pairs
- Synthesis section
- Next Steps section

**QA reports:**
- YAML frontmatter with required fields (date, author, topic, status)
- Context section
- Scope section (In Scope / Out of Scope)
- Test Cases with Steps, Expected, Actual, Status per case
- Evidence section (Screenshots, Videos, Logs, or External Links)
- Verdict section with overall status and summary

### Step 3: Content Analysis

Apply type-specific quality criteria:

**Research documents:**
- Are findings supported by specific file:line references?
- Are there claims without evidence from the codebase?
- Are there obvious areas that weren't investigated?
- Are open questions actually open (not answerable from the findings)?

**Plans:**
- Are there missing phases or gaps in the implementation sequence?
- Are success criteria specific enough to be actionable?
- Are there unstated assumptions about the codebase?
- Is error handling or rollback addressed?
- Is there scope creep risk (doing more than stated)?

**Brainstorms:**
- Were important angles left unexplored?
- Were conclusions reached prematurely before sufficient exploration?
- Are constraints and requirements clearly identified?
- Is the document ready to feed into research or planning?

**QA reports:**
- Does every test case have an actual result and status?
- Is evidence provided for failures (screenshots, logs)?
- Is the verdict consistent with individual test case results?
- Are edge cases and exploratory testing documented?
- Are external references (Sentry, PRs, CI) linked where relevant?

### Step 4: Gap Identification

Look for what's missing or assumed:
- Cross-reference claims against the codebase if needed (spawn **codebase-analyzer** agent for verification)
- Identify unstated assumptions
- Check for internal contradictions
- Verify external references are still valid

### Step 5: Present Findings

Categorize all findings into three severity levels:

| Severity | Meaning | Action |
|----------|---------|--------|
| **Critical** | Blocks correctness or completeness — must be addressed | Discuss with user |
| **Important** | Significant gap or weakness — should be addressed | Discuss with user (or auto-fix in Autopilot) |
| **Minor** | Formatting, typos, small inconsistencies | Auto-fix unless Verbose mode |

Present a summary as text output with findings grouped by severity.

### Step 6: Apply or Discuss

Based on output mode preference:

**If "Append errata":**
- Auto-fix Minor issues directly in the document
- For Critical/Important items, append a `## Review Errata` section at the end of the document with:
  ```
  ## Review Errata

  _Reviewed: YYYY-MM-DD by [reviewer]_

  ### Critical
  - [ ] [Finding description and recommended action]

  ### Important
  - [ ] [Finding description and recommended action]

  ### Resolved
  - [x] [Minor issue] — auto-fixed
  ```

**If "Separate file":**
- Write review to `thoughts/*/reviews/YYYY-MM-DD-review-of-<original-slug>.md`
- Include all findings with references back to the original document

### Step 7: Workflow Handoff

After the review is complete, determine the document type (from frontmatter, file path, or content) and propose the appropriate next step.

Use **AskUserQuestion** with context-dependent options:

**If reviewing a brainstorm document:**

| Question | Options |
|----------|---------|
| "Review complete. What's next for this brainstorm?" | 1. Start research (→ `/research`), 2. Create a plan directly (→ `/create-plan`), 3. Done for now |

**If reviewing a research document:**

| Question | Options |
|----------|---------|
| "Review complete. What's next for this research?" | 1. Create a plan (→ `/create-plan`), 2. Done for now |

**If reviewing a plan document:**

| Question | Options |
|----------|---------|
| "Review complete. What's next for this plan?" | 1. Start implementation (→ `/implement-plan`), 2. Done for now |

**If reviewing a post-implementation verification:**

| Question | Options |
|----------|---------|
| "Review complete. What's next?" | 1. Done — mark as complete, 2. Address remaining items |

**If reviewing a QA report:**

| Question | Options |
|----------|---------|
| "Review complete. What's next for this QA report?" | 1. Run post-QA verification (→ `/verify-plan`), 2. Address issues found, 3. Done |

**If document type is unclear**, ask a generic question:

| Question | Options |
|----------|---------|
| "Review complete. Would you like to proceed to the next workflow step?" | 1. Yes, suggest next step, 2. Done for now |

## No Rewriting Rule

**CRITICAL**: The reviewer identifies issues — the reviewer does NOT rewrite the document. Present findings and let the original author address them. The only exception is Minor auto-fixes (typos, formatting).

## Review Integration

If the `file-review` plugin is available and the user selected "Yes" during User Preferences setup:
- After the automated review, invoke `/file-review:file-review <path>` for inline human comments
- Process feedback with `file-review:process-review` skill
- If user selected "No" or autonomy mode is Autopilot, skip this step


ARGUMENTS: thoughts/taras/research/2026-03-18-workflow-redesign.md --autonomy=critical

---

<task-notification>
<task-id>aa4305ab7800ee30d</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-taras-worktrees-agent-swarm-2026-03-18-feat-workflow-redesign/063cc034-aa84-4605-a6b1-d3c0376e5c74/tasks/aa4305ab7800ee30d.output</output-file>
<status>completed</status>
<summary>Agent "Check referenced prior art" completed</summary>
<result>

Here are the findings, Taras.

---

## Document Existence Check

### 1. Content-Agent Workflow Engine Mechanics

**Path**: `/Users/taras/Documents/code/content-agent/thoughts/shared/research/2026-03-18-workflow-engine-mechanics.md`

**Status**: Exists. It is an 825-line research document covering the content-agent's workflow engine as a generic system: YAML-based workflow definitions, executor model (`f(input) -> output`), step types (prompt, bash, git_pr, imgflip), context chaining, validation/litmus tests, state management in SQLite, cooldowns, and extension points. The research doc at `thoughts/taras/research/2026-03-18-workflow-redesign.md` references it on line 11 as "Prior art."

### 2. Original Workflow Engine Design

**Path**: `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/thoughts/taras/research/2026-03-06-workflow-engine-design.md`

**Status**: Exists. It is a 446-line research document covering the original design for the agent-swarm workflow engine: n8n-style JSON blob DAG storage in SQLite, BFS execution model, node executor registry pattern, LLM-classify conditional nodes, property-match conditions, and trigger subscription model. The redesign doc references it on line 12 as "Supersedes."

---

## `scheduled_tasks` Table

**Status**: Exists in the codebase. It is defined in two migrations.

**Initial definition** -- `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/src/be/migrations/001_initial.sql`, lines 216-244. The table has these columns:

- `id` (TEXT PK), `name` (TEXT UNIQUE), `description`, `cronExpression`, `intervalMs`, `taskTemplate` (TEXT NOT NULL), `taskType`, `tags`, `priority`, `targetAgentId`, `enabled`, `lastRunAt`, `nextRunAt`, `createdByAgentId`, `timezone` (default UTC), `consecutiveErrors`, `lastErrorAt`, `lastErrorMessage`, `model`, `scheduleType` (CHECK: `recurring` or `one_time`), `createdAt`, `lastUpdatedAt`
- A CHECK constraint enforces that recurring schedules must have either `cronExpression` or `intervalMs`, while one_time schedules have no such requirement.

**Indexes** on `scheduled_tasks` (001_initial.sql, lines 369-372):
- `idx_scheduled_tasks_enabled` on `enabled`
- `idx_scheduled_tasks_nextRunAt` on `nextRunAt`
- `idx_scheduled_tasks_type` on `scheduleType`

**Migration 002** -- `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/src/be/migrations/002_one_time_schedules.sql` -- recreated the table using the rename-copy-drop pattern to add the `scheduleType` column and its CHECK constraint. The 001_initial.sql already includes this column (because it is the collapsed baseline), so migration 002 applies only to databases that existed before the migration system was introduced.

**Linkage to the redesign**: The research document at `thoughts/taras/research/2026-03-18-workflow-redesign.md` references `scheduled_tasks` in the trigger system design (line 397 and lines 406-407). The schedule trigger type uses a `scheduleId` field (`z.string().uuid()`) that references a row in the `scheduled_tasks` table. The design states: "The schedule's SOT is in `scheduled_tasks` -- the workflow just links to it."

Additionally, the `agent_tasks` table already has a `scheduleId TEXT` column (001_initial.sql, line 103) with an index (`idx_agent_tasks_schedule_id`, line 338), providing the existing linkage between tasks and schedules that the workflow engine schedule trigger would build upon.</result>
<usage><total_tokens>52320</total_tokens><tool_uses>10</tool_uses><duration_ms>48092</duration_ms></usage>
</task-notification>
Full transcript available at: /private/tmp/claude-501/-Users-taras-worktrees-agent-swarm-2026-03-18-feat-workflow-redesign/063cc034-aa84-4605-a6b1-d3c0376e5c74/tasks/aa4305ab7800ee30d.output

---

<task-notification>
<task-id>ad550dccf77ddcc4c</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-taras-worktrees-agent-swarm-2026-03-18-feat-workflow-redesign/063cc034-aa84-4605-a6b1-d3c0376e5c74/tasks/ad550dccf77ddcc4c.output</output-file>
<status>completed</status>
<summary>Agent "Verify workflow engine claims" completed</summary>
<result>I found the E2E scripts. Now I have all the data needed to compile the verification report. Let me tally everything up.

---

## Verification Report: Research Document Claims vs. Actual Codebase

### 1. Database Tables

**Claim (SS3.1):** `003_workflows.sql` and `004_workflow_source.sql` exist; tables `workflows`, `workflow_runs`, `workflow_run_steps` exist; `agent_tasks` has `workflowRunId` and `workflowRunStepId` columns.

**Verdict: MATCH**

- `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/src/be/migrations/003_workflows.sql` exists. It defines:
  - `workflows` table (line 2)
  - `workflow_runs` table (line 15)
  - `workflow_run_steps` table (line 32)
  - `ALTER TABLE agent_tasks ADD COLUMN workflowRunId` (line 49)
  - `ALTER TABLE agent_tasks ADD COLUMN workflowRunStepId` (line 50)

- `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/src/be/migrations/004_workflow_source.sql` exists. It recreates `agent_tasks` with the updated `source` CHECK constraint that includes `'workflow'` (line 15), and carries forward `workflowRunId` (line 43) and `workflowRunStepId` (line 44).

All table and column claims are accurate.

---

### 2. Engine Files

**Claim (SS3.1):** Eight files in `src/workflows/` with specific functions and behaviors.

**Verdict: MATCH** (all 8 files exist with the described functions)

| File | Claimed | Actual |
|------|---------|--------|
| `engine.ts` | `startWorkflowExecution()`, `walkDag()`, `executeNode()` | All three present at lines 38, 50, 124 |
| `resume.ts` | Event bus listeners for task.completed/failed/cancelled | `setupWorkflowResumeListener` at line 21, listens to `task.completed` (line 22), `task.failed` (line 28), `task.cancelled` (line 34) |
| `recovery.ts` | Stuck run detection | `recoverStuckWorkflowRuns()` at line 10, calls `getStuckWorkflowRuns()` (line 11) |
| `triggers.ts` | Event matching | `evaluateWorkflowTriggers()` at line 5, `matchTriggerNode()` at line 19 |
| `event-bus.ts` | EventEmitter | `InProcessEventBus` class at line 9, wraps `node:events` `EventEmitter` at line 1/10 |
| `template.ts` | `{{path.to.value}}` interpolation | `interpolate()` at line 5, regex `\{\{([^}]+)\}\}` at line 6 |
| `llm-provider.ts` | OpenRouter or Claude CLI | `OpenRouterProvider` at line 8, `ClaudeCliProvider` at line 27 |
| `index.ts` | `initWorkflows()` | Present at line 11, wires up resume listener and trigger events |

---

### 3. Node Executors

**Claim (SS3.1):** Six files in `src/workflows/nodes/` with specific behaviors.

**Verdict: MATCH**

| File | Claimed Mode | Actual Mode (per code) |
|------|-------------|----------------------|
| `create-task.ts` | Async: creates agent task, pauses workflow | Returns `mode: "async"` (line 30) |
| `delegate-to-agent.ts` | Async: assigns/offers task to agent | Returns `mode: "async"` (line 29) |
| `send-message.ts` | Instant: posts to channel | Returns `mode: "instant"` (line 18) |
| `property-match.ts` | Instant: JSONPath condition evaluation | Returns `mode: "instant"` (line 35) |
| `code-match.ts` | Instant: sandboxed JS evaluation | Returns `mode: "instant"` (line 49) |
| `llm-classify.ts` | Instant (awaited): AI SDK structured output | Returns `mode: "instant"` (lines 32, 39) |

All six files exist at `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/src/workflows/nodes/` with the described behaviors.

---

### 4. Trigger Types

**Claim (SS3.1):** 7 trigger types: `trigger-new-task`, `trigger-task-completed`, `trigger-webhook`, `trigger-email`, `trigger-slack-message`, `trigger-github-event`, `trigger-gitlab-event`.

**Verdict: MATCH**

All 7 appear in the `executeNode()` switch statement in `engine.ts` at lines 132-138, and in the `matchTriggerNode()` function in `triggers.ts` at lines 22-53:

1. `trigger-new-task` (engine.ts:132, triggers.ts:23)
2. `trigger-task-completed` (engine.ts:133, triggers.ts:29)
3. `trigger-webhook` (engine.ts:134, triggers.ts:49)
4. `trigger-email` (engine.ts:135, triggers.ts:45)
5. `trigger-slack-message` (engine.ts:136, triggers.ts:41)
6. `trigger-github-event` (engine.ts:137, triggers.ts:33)
7. `trigger-gitlab-event` (engine.ts:138, triggers.ts:37)

---

### 5. MCP Tools

**Claim (SS3.1):** 9 tools in `src/tools/workflows/`.

**Verdict: MATCH**

The `index.ts` barrel file exports exactly 9 tool registration functions (lines 1-9):

1. `create-workflow.ts`
2. `delete-workflow.ts`
3. `get-workflow.ts`
4. `get-workflow-run.ts`
5. `list-workflow-runs.ts`
6. `list-workflows.ts`
7. `retry-workflow-run.ts`
8. `trigger-workflow.ts`
9. `update-workflow.ts`

There are 10 files total in the directory (9 tool files + `index.ts`).

---

### 6. HTTP API

**Claim (SS3.1):** `src/http/workflows.ts` exists with "Full REST for workflows, runs, steps, retry, webhook trigger."

**Verdict: MATCH**

`/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/src/http/workflows.ts` defines 9 route objects and a single `handleWorkflows()` handler (line 145) covering:

| Route | Method | Path |
|-------|--------|------|
| List workflows | GET | `/api/workflows` (line 21) |
| Create workflow | POST | `/api/workflows` (line 32) |
| Get workflow | GET | `/api/workflows/{id}` (line 49) |
| Update workflow | PUT | `/api/workflows/{id}` (line 62) |
| Delete workflow | DELETE | `/api/workflows/{id}` (line 77) |
| Trigger workflow | POST | `/api/workflows/{id}/trigger` (line 90) |
| List runs | GET | `/api/workflows/{id}/runs` (line 105) |
| Get run + steps | GET | `/api/workflow-runs/{id}` (line 117) |
| Retry run | POST | `/api/workflow-runs/{id}/retry` (line 130) |

The trigger endpoint at line 238-243 does webhook secret verification via query param (`?secret=...`), not HMAC-SHA256 header verification. The doc's description of "webhook trigger" is accurate in that the endpoint exists, though the current auth is query-string secret comparison, not HMAC header-based.

---

### 7. Tests

**Claim (SS3.1):** "~2,600 lines across 8 test files + E2E script."

**Verdict: PARTIAL MISMATCH — 9 test files, not 8; ~2,521 lines, not ~2,600**

There are **9** test files (not 8):

| Test file | Lines |
|-----------|-------|
| `workflow-engine.test.ts` | 223 |
| `workflow-engine-errors.test.ts` | 173 |
| `workflow-http.test.ts` | 490 |
| `workflow-nodes-phase6.test.ts` | 255 |
| `workflow-property-match.test.ts` | 352 |
| `workflow-recovery.test.ts` | 155 |
| `workflow-resume-retry.test.ts` | 216 |
| `workflow-triggers.test.ts` | 419 |
| `gitlab-workflow-triggers.test.ts` | 238 |
| **Total** | **2,521** |

The E2E scripts exist at:
- `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/scripts/e2e-workflow-test.sh`
- `/Users/taras/worktrees/agent-swarm/2026-03-18-feat/workflow-redesign/scripts/e2e-workflow-test.ts`

The "E2E script" part of the claim is accurate. The line count is close (~2,521 vs. ~2,600) but not exact, and the file count is 9, not 8. Likely the `gitlab-workflow-triggers.test.ts` was added after the document was drafted, or the count was taken at a different point.

---

### 8. Context Checkpoint Gap (SS8.2)

**Claim (SS8.2):** "Context checkpoint after every step (currently only done on `waiting` -- also do it on each `completed`)."

**Verdict: MATCH — the claim about the current behavior is accurate**

In `engine.ts`, the `walkDag()` function:

- **On async (waiting) steps** (lines 80-85): Calls `updateWorkflowRun(runId, { status: "waiting", context: ctx })` -- context IS checkpointed.
- **On instant (completed) steps** (lines 89-94): Updates the step status and adds output to the in-memory `ctx` object, but does **NOT** call `updateWorkflowRun()` to persist the context. The only time context is persisted for completed steps is at the very end of the walk when the workflow is fully complete (lines 116-121).
- **On failure** (lines 98-110): Calls `updateWorkflowRun()` but only sets `status: "failed"` and `error` -- no `context` field is passed.

So the document's claim is precisely correct: context is persisted to the database only when the run enters `waiting` status (line 82-84) or final `completed` status (line 116-119). If the process crashes between two instant steps mid-walk, all accumulated context since the last checkpoint would be lost.

---

### Summary Table

| # | Claim | Verdict |
|---|-------|---------|
| 1 | DB tables: `003_workflows.sql`, `004_workflow_source.sql`, tables + columns | **MATCH** |
| 2 | 8 engine files with specific functions | **MATCH** (all 8 exist, all named functions confirmed) |
| 3 | 6 node executor files with specific modes | **MATCH** |
| 4 | 7 trigger types | **MATCH** |
| 5 | 9 MCP tools | **MATCH** |
| 6 | HTTP API with full REST | **MATCH** (9 endpoints confirmed) |
| 7 | ~2,600 lines across 8 test files + E2E | **PARTIAL MISMATCH** -- 9 files (not 8), ~2,521 lines (not ~2,600); E2E scripts confirmed |
| 8 | Context checkpoint only on `waiting` | **MATCH** -- confirmed at `engine.ts:80-85` vs. `engine.ts:89-94` |</result>
<usage><total_tokens>49459</total_tokens><tool_uses>37</tool_uses><duration_ms>100441</duration_ms></usage>
</task-notification>
Full transcript available at: /private/tmp/claude-501/-Users-taras-worktrees-agent-swarm-2026-03-18-feat-workflow-redesign/063cc034-aa84-4605-a6b1-d3c0376e5c74/tasks/ad550dccf77ddcc4c.output

---

<task-notification>
<task-id>bip4s7mw2</task-id>
<tool-use-id>toolu_01WGEUtEhNozUyWedi73Ajru</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-taras-worktrees-agent-swarm-2026-03-18-feat-workflow-redesign/063cc034-aa84-4605-a6b1-d3c0376e5c74/tasks/bip4s7mw2.output</output-file>
<status>completed</status>
<summary>Background command "Launch file-review GUI for inline comments" completed (exit code 0)</summary>
</task-notification>
Read the output file to retrieve the result: /private/tmp/claude-501/-Users-taras-worktrees-agent-swarm-2026-03-18-feat-workflow-redesign/063cc034-aa84-4605-a6b1-d3c0376e5c74/tasks/bip4s7mw2.output