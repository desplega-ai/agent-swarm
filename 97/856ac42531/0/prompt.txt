Research current TypeScript usage for badlogic/pi-mono and related npm packages (@mariozechner/pi-agent-core, pi-coding-agent, pi-ai). I need concrete API examples for creating an embedded agent session, subscribing to lifecycle/tool/message events, running a prompt, capturing usage/cost fields, cancelling a run, and resuming session by session id. Return package names, import paths, key types/functions with signatures if possible, and one minimal runnable pseudocode snippet.